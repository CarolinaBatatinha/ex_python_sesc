# Faça o cálculo da área de um retângulo.

base = float(input('Qual a medida da base em cm?: '))
altura = float(input('Qual a medida da altura em cm?: '))

area = base * altura

print('A área do retângulo mede {0}cm².'.format(area))